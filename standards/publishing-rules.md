# Publishing Rules

## Rule of One
Every fact should exist in one source entity and be reused across outputs.

## Publishing Flow
Source entity → review → approved → outputs generated → published → review scheduled.
